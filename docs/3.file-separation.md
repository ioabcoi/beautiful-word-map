# 📁 Beautiful-Word-Map 파일 분리 가이드

---

## 최종 구조

```
src/
├── api/
│   └── index.js            ← fetch 함수 모음
├── constants/
│   └── flagMap.js          ← FLAG_MAP, THEME_COLORS, themeColor
├── styles/
│   └── GlobalStyle.jsx     ← FontLoader, GlobalStyle
├── components/
│   ├── Icons.jsx           ← Ico (Plus, Edit, Trash, Close, Chevron)
│   ├── Dropdown.jsx
│   ├── WordCard.jsx
│   ├── WordForm.jsx
│   └── ThemeManager.jsx
└── App.jsx                 ← 상태관리 + 레이아웃만
```

---

## 적용 방법

기존 `src/App.jsx` 파일을 **삭제**하고, (_App.jsx)
다운받은 파일들을 위 구조에 맞게 배치하면 끝.

---

## 각 파일 역할

| 파일 | 역할 |
|------|------|
| `api/index.js` | 서버와 통신하는 fetch 함수 모음 (getWords, addWord 등) |
| `constants/flagMap.js` | 언어별 국기 이모지, 테마 색상 상수 |
| `styles/GlobalStyle.jsx` | 전역 CSS 변수, 폰트 로더, 애니메이션 |
| `components/Icons.jsx` | 아이콘 SVG 컴포넌트 모음 |
| `components/Dropdown.jsx` | 언어별 / 테마별 필터 드롭다운 |
| `components/WordCard.jsx` | 단어 카드 (확장 시 예시 문장 + 수정/삭제) |
| `components/WordForm.jsx` | 단어 추가 / 수정 슬라이드업 폼 |
| `components/ThemeManager.jsx` | 테마 추가 / 삭제 모달 |
| `App.jsx` | 상태관리 + 전체 레이아웃 |

---

### data.json 수동 업데이트 시

```
# Ctrl+C 로 서버 종료 후
npm run start
```